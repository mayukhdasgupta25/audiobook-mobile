import type { Ionicons } from '@expo/vector-icons';

/** Consistent back control across stack screens (chevron, not arrow stem). */
export const APP_BACK_ICON: keyof typeof Ionicons.glyphMap = 'chevron-back';

export const APP_BACK_ICON_SIZE = 22;
