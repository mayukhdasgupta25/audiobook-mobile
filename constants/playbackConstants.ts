/** Prefetch next chapter HLS playlists when this many seconds remain in the current chapter. */
export const NEXT_CHAPTER_PREFETCH_REMAINING_SEC = 45;

/** Position-based chapter-end trigger fires within this margin of the seekable end (seconds). */
export const CHAPTER_END_POSITION_MARGIN_SEC = 0.5;
